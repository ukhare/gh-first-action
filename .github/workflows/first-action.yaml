name: my first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: echo "Hello Ubuntu"
      - name: bye greeting
        run: echo "Bye Ubuntu"
